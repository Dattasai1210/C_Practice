#include<stdio.h>
void min(int arr[])
{
    int min=arr[0];
    for(int i=0;i<5;i++)
    {
        if(arr[i]<min)
        {
            min=arr[i];
        }
    }
    printf("Min Element:%d",min);
}
int main()
{
    int arr[]={1,2,3,4,5};
    min(arr);
}
